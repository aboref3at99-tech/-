import { APP_NAME, APP_VERSION } from '@restaurant/shared';

console.log(`${APP_NAME} desktop bootstrap started.`);
console.log(`Version: ${APP_VERSION}`);
console.log('Next: wire Tauri shell + React renderer + secure IPC commands.');
